# Language Status

This revision is `0.11.0` of the `v0.11` documentation set.

The current state of the docs is:

- The old numbered chapter layout has been migrated into the new pages.
- The new navigable structure is the active documentation layout for this version.
- The standard-library area is now split out as its own documentation zone.

Name status:

- The language name remains pending.
- The specification still uses `Element` as the working name.
- Toolchain names remain `pyx`, `pvm`, and `.pbc`.

Draft-status notes:

- The standard-library pages intentionally include declaration-style surfaces even where
  implementation details are still incomplete.
- When a boundary is not final, the draft marks it explicitly instead of presenting it as
  settled.
- Open issues remain tracked in [[open-questions]].
