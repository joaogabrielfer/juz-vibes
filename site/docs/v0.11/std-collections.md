# std::collections

This page is a placeholder for the standard collection surface.

Collection names such as `Arr<T>`, `Set<T>`, `Stream<T>`, and related constructors are
referenced by other draft pages, but their full declaration surface is not specified in
this revision.

See [[standard-library]] and [[std-effects]] for current collection-related references.
